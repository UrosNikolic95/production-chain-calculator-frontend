// Development environment (used by `ng serve` / build --configuration development).
// Points directly at the locally running backend.
export const environment = {
  production: false,
  apiBase: 'http://localhost:3000/api',
};
